# VAD runtime (Apple SiliconではMPSを使用可能)
torch
torchaudio
numpy

# Input & audio
pynput
sounddevice
pyperclip
pyyaml

# VAD
silero-vad
# Silero VAD の ONNX を CPU 実行する。onnxruntime は silero-vad の optional extra
# （onnx-cpu）でしか入らないため、明示依存として宣言する。これが無いと
# vad.py の import onnxruntime が失敗し VAD が常に無効化される（無音圧縮・長文分割が効かない）。
onnxruntime>=1.16.1

# Optional but recommended
ffmpeg-python

# GUI & Packaging
PySide6
pyinstaller

# Groq API (Cloud transcription)
groq>=0.4.0

# OpenAI API (GPT-4o Transcription & LLM post-processing - optional)
openai>=1.0.0

# HTTP client tuning for API calls
httpx>=0.27.0

# Deepgram リアルタイムストリーミング（WebSocket、同期クライアント）
websockets>=13.0

# Cerebras API (LLM post-processing - optional)
cerebras-cloud-sdk>=1.0.0

# Environment variable management
python-dotenv>=1.0.0

# Secret storage (macOS Keychain / Windows Credential Manager)
keyring>=24.0

# 自動アップデートの Ed25519 署名検証（Windows 配布版の改竄対策）
cryptography>=42.0

# macOS native APIs (Dock 非表示制御で AppKit を使用)
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
